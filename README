##WiFiDog build
1. ingress the top dir ;
2. excute the shell script `./build.sh -l`,list the support product name;
3. excute the shell script `./build.sh product name` will begine build the wifidog;
4. when compile succed ,the result will be tar in dir `upgrade`;

##优化内容包括
1. 优化iptables portal 框架，不适用iptables mark 数据包方式；
2. 支持设备上下线wifi 日志文件；
3. 支持网络下发黑白名单，json 数据流交互；
4. 支持多家主流光猫，中兴，华为，大唐，烽火，中国移动，锐捷，创维直接编译后，配合wifidog服务器可使用；
5. 增强wifidog 稳定性；
6. 支持wdctl 操控wifidog，适配多家光猫设备；
7. 降低wifidog 服务器负载，通过调整单portal 至批量请求服务器；
8. 增加设备认证后，kick 机制；

##即将增加功能
1. 优化wifidog 程序框架，将采用epoll 方式处理socket event；
